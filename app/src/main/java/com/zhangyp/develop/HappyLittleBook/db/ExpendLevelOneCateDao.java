package com.zhangyp.develop.HappyLittleBook.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.zhangyp.develop.HappyLittleBook.bean.ExpendLevelOneCate;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "EXPEND_LEVEL_ONE_CATE".
*/
public class ExpendLevelOneCateDao extends AbstractDao<ExpendLevelOneCate, Long> {

    public static final String TABLENAME = "EXPEND_LEVEL_ONE_CATE";

    /**
     * Properties of entity ExpendLevelOneCate.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property CateName = new Property(1, String.class, "cateName", false, "CATE_NAME");
    }


    public ExpendLevelOneCateDao(DaoConfig config) {
        super(config);
    }
    
    public ExpendLevelOneCateDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"EXPEND_LEVEL_ONE_CATE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"CATE_NAME\" TEXT UNIQUE );"); // 1: cateName
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"EXPEND_LEVEL_ONE_CATE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ExpendLevelOneCate entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String cateName = entity.getCateName();
        if (cateName != null) {
            stmt.bindString(2, cateName);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ExpendLevelOneCate entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String cateName = entity.getCateName();
        if (cateName != null) {
            stmt.bindString(2, cateName);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ExpendLevelOneCate readEntity(Cursor cursor, int offset) {
        ExpendLevelOneCate entity = new ExpendLevelOneCate( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // cateName
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ExpendLevelOneCate entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCateName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ExpendLevelOneCate entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ExpendLevelOneCate entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ExpendLevelOneCate entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
